package rules.movement;

import com.interdimensionalmallet.twtpthvtt.model.*;

dialect "mvel";

rule "Start Movement Query"
    when
        $thing: Thing()
        $resource: Resource(thingId == $thing.id)
    then
        System.out.println("Start Movement Query");
end